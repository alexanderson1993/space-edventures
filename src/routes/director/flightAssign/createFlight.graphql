mutation FlightRecordCreate(
  $centerId: ID!
  $flightId: ID!
  $flightType: ID!
  $simulators: [FlightSimulatorInput!]!
) {
  flightRecordCreate(
    centerId: $centerId
    thoriumFlightId: $flightId
    flightTypeId: $flightType
    simulators: $simulators
    silent: true
  ) {
    id
  }
}
