mutation BadgeCreate($name: String!, $image: Upload, $description: String) {
  missionCreate: badgeCreate(
    badge: {
      name: $name
      image: $image
      description: $description
      type: mission
    }
  ) {
    id
    name
    description
    type
    image
  }
}
