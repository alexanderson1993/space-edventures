query GetFlights($centerId: ID!) {
  flightRecords(centerId: $centerId) {
    id
    flightType {
      id
      name
    }
    simulators {
      id
      name
      stations {
        name
      }
    }
  }
}
