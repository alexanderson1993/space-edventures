import React from "react";
import { Button } from "../../../components";

const PhotoVerify = ({ back }) => {
  return (
    <div>
      <div>
        <Button onClick={back}>Go Back</Button>
      </div>
      Hi
    </div>
  );
};
export default PhotoVerify;
