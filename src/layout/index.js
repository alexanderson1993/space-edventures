import React from 'react';

const Layout = (props) => {
    // Layout includes
    // - Global navigation
    // - User Profile link
    // - Frame
    // It should also be changable, in case we 
    // feel like mixing it up.
    return <div className="layout-line">
    </div>
}
export default Layout;